version: '3.9'

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 13o1714QO^WXep@A
      POSTGRES_USER: cherrypop
      POSTGRES_DB: fleek
    volumes:
      - ./pgdata:/var/lib/postgresql/data
